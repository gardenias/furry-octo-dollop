<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.p</groupId>
        <artifactId>p-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.p.quant</groupId>
    <artifactId>quant-gateway-project</artifactId>
    <version>${revision}</version>

    <packaging>pom</packaging>

    <modules>
        <module>endpoints</module>
        <module>apps</module>
    </modules>

    <properties>
        <revision>0.0.1-SNAPSHOT</revision>
        <base.version>${revision}</base.version>

        <IMAGE.BUILD.SKIP>true</IMAGE.BUILD.SKIP>
        <java.ops.mem>-Xms512m -Xmx1g</java.ops.mem>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.p.quant.endpoints</groupId>
                <artifactId>endpoints-okx</artifactId>
                <version>${base.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
